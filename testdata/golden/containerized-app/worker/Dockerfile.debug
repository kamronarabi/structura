# The same service with a debugger in it. A variant, not a second component.
FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir debugpy
COPY . .
ENV REDIS_URL=redis://cache:6379/0
EXPOSE 5678
CMD ["python", "-m", "debugpy", "--listen", "0.0.0.0:5678", "-m", "worker"]
