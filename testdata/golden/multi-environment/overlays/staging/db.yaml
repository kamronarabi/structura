apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db
  namespace: staging
spec:
  serviceName: db
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
        - name: postgres
          image: postgres:16
